<UserControl x:Class="library.Controls.CategoriesTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
                 xmlns:mah="http://metro.mahapps.com/winfx/xaml/iconpacks">


    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/Themes/CustomersTable.Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>
</ResourceDictionary>
    </UserControl.Resources>

    
    <Grid>
        <DataGrid ItemsSource="{Binding Categorias}"
                  ColumnHeaderStyle="{StaticResource SlimHeader}"
  CellStyle="{StaticResource CenterCell}"
  HorizontalGridLinesBrush="{DynamicResource RowSeparator}"
                
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  Background="{DynamicResource App.CardBrush}">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow" BasedOn="{StaticResource SlimRow}" />
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdCategoria}" Width="Auto"/>
                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>

                <!-- ACCIÓN -->
                <DataGridTemplateColumn Header="Acción" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <!-- Editar -->
                                <Button Style="{StaticResource GhostIconButton}" 
                                        Command="{Binding DataContext.EditCategoriaCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}">
                                    <mah:PackIconMaterial Kind="SquareEditOutline" Width="18" Height="18"/>
                                </Button>
                                <!-- Eliminar -->
                                <Button Style="{StaticResource GhostIconButton}" 
                                        Command="{Binding DataContext.DeleteCategoriaCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}">
                                    <mah:PackIconMaterial Kind="TrashCanOutline" Width="18" Height="18"/>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
