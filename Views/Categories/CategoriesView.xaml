<UserControl x:Class="library.Views.Categories.CategoriesView"           
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrls="clr-namespace:library.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">

    <Grid Margin="2 0 2 8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Stats -->
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <!-- Contenedor tarjeta -->
        </Grid.RowDefinitions>

        <!-- Stats -->
        <ctrls:StatsSummary Grid.Row="0" ItemsSource="{Binding Stats}" />

        <materialDesign:Card Grid.Row="2"
                         Margin="2 0 4 8"
                         Background="{DynamicResource Brush.Card}"
                         BorderBrush="{DynamicResource Brush.Border}"
                         BorderThickness="1"
                         UniformCornerRadius="10"
                         VerticalAlignment="Stretch">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <ctrls:CategoriesHeader Grid.Row="0"/>

                <!-- Tabla -->
                <ctrls:CategoriesTable Grid.Row="1"/>
            </Grid>
        </materialDesign:Card>
    </Grid>
</UserControl>
