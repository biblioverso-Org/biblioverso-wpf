<UserControl x:Class="library.Dialogs.AddCustomerDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:conv="clr-namespace:library.Converters"
             MinWidth="600" MaxWidth="760">

    <!-- ================== RESOURCES ================== -->
    <UserControl.Resources>
        <conv:SafeImageSourceConverter x:Key="SafeImg"/>

        <!-- estilos de botones redondeados -->
        <Style x:Key="PillButtonBase" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="MinWidth" Value="140"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource App.BorderBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource App.TextPrimaryBrush}"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>

        <Style x:Key="PillPrimaryButton" TargetType="Button" BasedOn="{StaticResource PillButtonBase}">
            <Setter Property="Background"  Value="{DynamicResource App.PrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource App.PrimaryBrush}"/>
            <Setter Property="Foreground"  Value="White"/>
        </Style>

        <Style x:Key="PillOutlineButton" TargetType="Button" BasedOn="{StaticResource PillButtonBase}" />
    </UserControl.Resources>

    <!-- ================== CONTENT ================== -->
    <Border Padding="24,20,24,16" Background="{DynamicResource App.CardBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Título -->
            <TextBlock Grid.Row="0" Text="Agregar cliente"
                 FontSize="20" FontWeight="SemiBold" Margin="4,0,4,16"
                 Foreground="{DynamicResource App.TextPrimaryBrush}"/>

            <!-- Contenido -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="148"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Avatar -->
                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <Grid Width="112" Height="112">
                        <Ellipse Fill="{DynamicResource App.ElevatedBrush}"/>
                        <Ellipse>
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Foto}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Foto}" Value="">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                            <Ellipse.Fill>
                                <ImageBrush Stretch="UniformToFill"
                                            ImageSource="{Binding Foto, Converter={StaticResource SafeImg}}"/>
                            </Ellipse.Fill>
                        </Ellipse>
                        <mah:PackIconMaterial Kind="AccountCircle" Width="64" Height="64"
                                              Foreground="{DynamicResource App.TextMutedBrush}">
                            <mah:PackIconMaterial.Style>
                                <Style TargetType="mah:PackIconMaterial">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Foto}" Value="{x:Null}">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding Foto}" Value="">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </mah:PackIconMaterial.Style>
                        </mah:PackIconMaterial>
                    </Grid>

                    <Button Style="{StaticResource PillOutlineButton}" Margin="0,12,0,0"
                            Command="{Binding BrowseImageCommand}">
                        <StackPanel Orientation="Horizontal">
                            <mah:PackIconMaterial Kind="ImagePlus" Width="18" Height="18" Margin="0,0,8,0"/>
                            <TextBlock Text="Seleccionar foto"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <!-- Formulario -->
                <Grid Grid.Column="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                

                    <!-- Nombre / Apellido -->
                    <TextBox Grid.Row="1" Grid.Column="0"
                             materialDesign:HintAssist.Hint="Nombre"
                             Text="{Binding Nombre}" Margin="0,0,8,10"/>
                    <TextBox Grid.Row="1" Grid.Column="1"
                             materialDesign:HintAssist.Hint="Apellido"
                             Text="{Binding Apellido}" Margin="8,0,0,10"/>

                    <!-- Email -->
                    <TextBox Grid.Row="2" Grid.ColumnSpan="2"
                             materialDesign:HintAssist.Hint="Email"
                             Text="{Binding Email}" Margin="0,0,0,10"/>

                    <!-- Teléfono / Usuario -->
                    <TextBox Grid.Row="3" Grid.Column="0"
                             materialDesign:HintAssist.Hint="Teléfono"
                             Text="{Binding Telefono}" Margin="0,0,8,10"/>
                    <TextBox Grid.Row="3" Grid.Column="1"
                             materialDesign:HintAssist.Hint="Usuario"
                             Text="{Binding UserName}" Margin="8,0,0,10"/>

                    <!-- Contraseña / Dirección -->
                    <PasswordBox Grid.Row="4" Grid.Column="0" Margin="0,0,8,10"
             materialDesign:HintAssist.Hint="Contraseña"
             PasswordChanged="PasswordBox_PasswordChanged"/>
                    
                    <TextBox Grid.Row="4" Grid.Column="1"
                             materialDesign:HintAssist.Hint="Dirección"
                             Text="{Binding Direccion}" Margin="8,0,0,10"/>

                    <!-- Fecha de Nacimiento / Nacionalidad -->
                    <DatePicker Grid.Row="5" Grid.Column="0"
                                materialDesign:HintAssist.Hint="Fecha de Nacimiento"
                                SelectedDate="{Binding FechaNacimiento}" Margin="0,0,8,10"/>
                    <TextBox Grid.Row="5" Grid.Column="1"
                             materialDesign:HintAssist.Hint="Nacionalidad"
                             Text="{Binding Nacionalidad}" Margin="8,0,0,10"/>

                    <!-- Biografía -->
                    <TextBox Grid.Row="6" Grid.ColumnSpan="2"
                             AcceptsReturn="True" Height="80"
                             materialDesign:HintAssist.Hint="Biografía"
                             Text="{Binding Biografia}" Margin="0,0,0,10"
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                    <ComboBox Grid.Row="7" Grid.ColumnSpan="2"
          ItemsSource="{Binding Genders}"
          SelectedItem="{Binding Genero, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
          materialDesign:HintAssist.Hint="Género"/>

                </Grid>
            </Grid>

            <!-- Footer -->
            <DockPanel Grid.Row="2" LastChildFill="False" Margin="0,12,0,0">
                <Button Style="{StaticResource PillPrimaryButton}" DockPanel.Dock="Right"
                        IsEnabled="{Binding IsValid}"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="{Binding}">
                    <StackPanel Orientation="Horizontal">
                        <mah:PackIconMaterial Kind="ContentSaveOutline" Width="18" Height="18" Margin="0,0,8,0"/>
                        <TextBlock Text="Guardar"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource PillOutlineButton}" DockPanel.Dock="Right" Margin="8,0,0,0"
                        Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                        CommandParameter="{x:Null}">
                    <StackPanel Orientation="Horizontal">
                        <mah:PackIconMaterial Kind="Close" Width="18" Height="18" Margin="0,0,8,0"/>
                        <TextBlock Text="Cancelar"/>
                    </StackPanel>
                </Button>
            </DockPanel>
        </Grid>
    </Border>
</UserControl>
